version: '3'
services:
  world-db:
    build: ../../../../Downloads/world-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: world
      MYSQL_ROOT_HOST: '%'
    ports:
      - "33061:3306"

